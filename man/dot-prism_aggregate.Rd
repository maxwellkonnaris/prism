% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate.R
\name{.prism_aggregate}
\alias{.prism_aggregate}
\title{Aggregate accepted bootstrap draws into CI matrices and pairwise tables}
\usage{
.prism_aggregate(
  draws,
  feature_names = NULL,
  ci_alpha = 0.05,
  delta = 0.1,
  p_adjust_method = "BH"
)
}
\arguments{
\item{draws}{Output of `.prism_bootstrap()`: `list(lower, upper, rel)`,
each a D x D x S_eff array over accepted draws.}

\item{feature_names}{Character vector of length D, or `NULL`.}

\item{ci_alpha}{Tail probability for CI endpoints.}

\item{delta}{Half-width of the pairwise null region used for p-values.}

\item{p_adjust_method}{Passed to `stats::p.adjust()`.}
}
\value{
`list(ci_lower, ci_upper, pairwise, pairwise_rel)`.
}
\description{
`ci_lower`/`ci_upper` are the empirical `ci_alpha/2` and
`1 - ci_alpha/2` quantiles of the accepted per-draw lower/upper bound
arrays -- for a single deterministic draw (`S_eff == 1`), the quantile
of one value is that value, so this reduces automatically to the
identification interval with no special case needed. `p_value` is the
one genuine special case: with only one draw there is no bootstrap
distribution to test against the null region, so it is reported as
`NA` rather than a degenerate one-sample value.
}
\keyword{internal}
