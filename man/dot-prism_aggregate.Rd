% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate.R
\name{.prism_aggregate}
\alias{.prism_aggregate}
\title{Aggregate bootstrap draws into CI matrices and pairwise tables}
\usage{
.prism_aggregate(
  draws,
  feature_names = NULL,
  ci_alpha = 0.05,
  delta = 0,
  bootstrap_active = TRUE,
  p_adjust_method = "BH"
)
}
\arguments{
\item{draws}{Output of `.prism_bootstrap()`.}

\item{feature_names}{Character vector of length D, or `NULL`.}

\item{ci_alpha}{Tail probability for CI endpoints.}

\item{delta}{Half-width of the pairwise null region used for p-values.}

\item{bootstrap_active}{Whether paired subject bootstrapping was performed.}

\item{p_adjust_method}{Passed to `stats::p.adjust()`.}
}
\value{
`list(ci_lower, ci_upper, pairwise, pairwise_rel)`.
}
\description{
`ci_lower`/`ci_upper` are the empirical `ci_alpha/2` and
`1 - ci_alpha/2` quantiles of the per-draw lower/upper bound
arrays -- for a single deterministic draw (`S_eff == 1`), the quantile
of one value is that value, so this reduces automatically to the
identification interval with no special case needed. `p_value` is the
reported only when paired subject bootstrapping is active. Without subject
bootstrapping, `p_value` and `q_value` are `NA`, while `covers_zero` reports
whether the aggregated identification interval includes zero.
}
\keyword{internal}
