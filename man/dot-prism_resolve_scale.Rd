% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prism.R
\name{.prism_resolve_scale}
\alias{.prism_resolve_scale}
\title{Resolve the `scale` argument (plus its shorthands) into a bootstrap-ready form}
\usage{
.prism_resolve_scale(scale, N, sigma_L, sigma_U, rho_L, rho_U)
}
\value{
`list(scale_mode, sigma_L, sigma_U, rho_L, rho_U)`, where
  `scale_mode` is ready to pass to `.prism_bootstrap()` and the
  `sigma_L`/etc. are the fixed-bounds values (or all `NULL` when
  `scale_mode$mode == "data"`).
}
\description{
Resolve the `scale` argument (plus its shorthands) into a bootstrap-ready form
}
\keyword{internal}
