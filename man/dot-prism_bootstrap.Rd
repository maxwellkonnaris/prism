% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{.prism_bootstrap}
\alias{.prism_bootstrap}
\title{Run the full PRISM bootstrap loop}
\usage{
.prism_bootstrap(
  counts,
  estimator,
  scale_mode,
  sigma_L = NULL,
  sigma_U = NULL,
  rho_L = NULL,
  rho_U = NULL,
  bootstrap = "both",
  S = 1000L,
  max_attempts_per_draw = 100L,
  seed = 1L,
  verbose = FALSE
)
}
\arguments{
\item{counts}{Features-by-samples count matrix (post-filtering).}

\item{estimator}{A `prism_composition_estimator`.}

\item{scale_mode}{A list describing the active scale-information mode:
`list(mode = "none")`, `list(mode = "scale_log", scale_log = ..., ci_level = ..., estimate_rho = ..., lower_zero = ...)`,
or `list(mode = "scale", scale_mat = ..., ci_level = ..., estimate_rho = ..., lower_zero = ...)`.}

\item{sigma_L, sigma_U, rho_L, rho_U}{Fixed bounds, used when `scale_mode$mode == "none"`.}

\item{bootstrap}{One of `"none"` or `"both"` (see `.resolve_bootstrap_mode()`).}

\item{S}{Requested number of draws.}

\item{max_attempts_per_draw}{Retry budget per requested draw.}

\item{seed}{Reproducibility seed, or `NULL`.}

\item{verbose}{Emit progress messages.}
}
\value{
A list with `lower`, `upper`, `rel` (D x D x S_eff arrays over
  accepted draws), and `diagnostics` (sampling accept/reject bookkeeping).
}
\description{
Resamples samples (paired across counts and scale, per `bootstrap`) and
draws compositions, retrying rejected draws up to
`max_attempts_per_draw` times, and returns every accepted draw's bound
matrices for `aggregate.R` to summarize.
}
\keyword{internal}
