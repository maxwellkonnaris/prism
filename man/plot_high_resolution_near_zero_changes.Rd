% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bounds.R
\name{plot_high_resolution_near_zero_changes}
\alias{plot_high_resolution_near_zero_changes}
\title{Plot standard and high-resolution intervals for refined pairs}
\usage{
plot_high_resolution_near_zero_changes(
  x,
  near_zero_threshold = NULL,
  delta = NULL
)
}
\arguments{
\item{x}{A PRISM result containing `high_resolution_diagnostics`, or the
diagnostics list itself.}

\item{near_zero_threshold}{Optional non-negative covariance-distance
threshold used to restrict the plot to pairs whose conservative interval
has an endpoint near zero. Default `NULL` displays all refined pairs.}

\item{delta}{Optional non-negative null-region half-width. When supplied,
dashed vertical lines mark `-delta` and `delta`.}
}
\value{
A `ggplot` object.
}
\description{
Shows conservative and high-resolution intervals for taxon pairs refined by
the high-resolution solver.
}
