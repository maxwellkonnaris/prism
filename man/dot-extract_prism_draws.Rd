% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prism.R
\name{.extract_prism_draws}
\alias{.extract_prism_draws}
\title{Extract every accepted draw's lower/upper/rel values, pair-major}
\usage{
.extract_prism_draws(draws, feature_names)
}
\arguments{
\item{draws}{Output of `.prism_bootstrap()`.}

\item{feature_names}{Character vector of length D.}
}
\value{
`list(lower, upper, rel)`, each an S_eff x n_pairs matrix, and
  `pair_index` (a data frame with `i`, `j`, `feature_i`, `feature_j`).
}
\description{
Reads from the in-memory arrays or, for a streamed run, straight from
the temporary files (still open at this point -- called before the
stream cleanup `on.exit()` fires).
}
\keyword{internal}
